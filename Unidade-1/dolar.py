#coding: utf-8
#@Crispiniano 
#Unidade 1: Calcula Dólar

real = float(raw_input('Reais? '))
cotacao = float(raw_input('Cotação? '))

conversao = real * cotacao 

print 'Conversão: $ {}'.format(conversao)
